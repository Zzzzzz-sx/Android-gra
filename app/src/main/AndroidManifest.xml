<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--  网络请求-->
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <!--  相机-->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 存储权限-->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <activity
            android:name=".Login.Login_doc"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Doc_shouye"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".Register.Register_doc"/>
        <activity android:name=".News.CreateNews"></activity>
        <activity android:name=".News.ShowNews"></activity>
        <activity android:name=".Login.Login_pat"></activity>
        <activity android:name=".Register.Register_pat"></activity>
        <activity android:name=".Pat_shouye" />
        <activity android:name=".Pat_Personalinfo"></activity>
        <activity android:name=".Doc_workcount" />


    </application>

</manifest>